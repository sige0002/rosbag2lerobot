{
  "name": "bagel-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend for `bagel ui` — config-driven ROS2 rosbag to LeRobot Dataset converter.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
